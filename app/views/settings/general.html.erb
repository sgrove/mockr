<% @title = "Settings" %>

<%= stylesheet_link_tag "settings/general" %>

<%= render :partial => "/shared/subnav",
           :locals => {
             :header => "Settings",
             :links => [["General", general_settings_path], ["Users", collaborators_path]]
           } %>

<div id="tabbed_content">
  <% form_for :setting, @setting, :url => setting_path(@setting),
    :html => {:method => :put} do |f| %>
    <%= f.hidden_field :key %>
    <%= f.label :value, "Bushido DNS:" %>
    <div class="field_row">
      <%= f.text_field :value %>
      <%= f.submit "Save" %>
    </div>
  <% end %>
</div>
